import VenueNotFoundComponent from "./not-found-menu";

export default function VenueNotFound() {
    return (
        <div className="h-full w-full flex items-center justify-center">
            <VenueNotFoundComponent />
        </div>
    )
}